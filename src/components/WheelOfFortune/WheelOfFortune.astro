---
import styles from './styles.css?url';

interface Props {
	items: string[];
}

const { items } = Astro.props;
---
<wheel-of-fortune>
	<template shadowrootmode="open">
		<link rel="stylesheet" href={styles} />
		<fieldset>
			<legend>The Wheel of Fork-tune</legend>

			<header>
				<blockquote>Don't know what to eat? Spin and find out!</blockquote>

				<aside>
					<button type="button" id="show-list">Show list</button>
					<button type="button" id="show-wheel">Show wheel</button>
				</aside>
			</header>

			<div id="wheel-container">
				<svg id="wheel-svg" viewBox="0 0 500 500" width="500" height="500">
					<g id="wheel-group" style="transform-origin: 250px 250px; transition: transform 0.5s ease-out" />
				</svg>
				<ol id="items-list"></ol>
				<hr id="wheel-marker" />
				<button type="button" id="wheel-spin">Spin the wheel</button>
				<button type="button" id="list-spin">Select from list</button>
			</div>
		</fieldset>

		<dialog>
			<h2>Results are in!</h2>
			<form method="dialog">
				<button>Close</button>
			</form>
		</dialog>

		<div hidden>
			<slot is:inline></slot>
		</div>
	</template>

	{items.map((item) => <span>{item}</span>)}
</wheel-of-fortune>
<script src="./index.ts"></script>
